{
  "name": "@pi-workflow/v2-domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "typecheck:contract": "tsc --project test/type-contract/tsconfig.json",
    "typecheck": "npm run build && npm run typecheck:contract",
    "test": "npm run build && npm run typecheck:contract && node --test test/*.test.mjs"
  }
}
