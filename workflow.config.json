{
  "$comment": "workflow 扩展配置。修改 activeModelProfile 后重启 pi 或 /reload 生效。每个角色统一配置 model(provider/model)与 effort。",
  "activeModelProfile": "gpt56",
  "modelProfiles": {
    "gpt56": {
      "$comment": "GPT-5.6 分工:Sol 主 session/PRD,Terra 单 task 编码,Luna 代码与最终审查；main=xhigh,PRD/dev=high,审查=xhigh。",
      "main": { "model": "codex2api/gpt-5.6-sol", "effort": "xhigh" },
      "prd": { "model": "codex2api/gpt-5.6-sol", "effort": "high" },
      "dev": { "model": "codex2api/gpt-5.6-terra", "effort": "high" },
      "reviewer": { "model": "codex2api/gpt-5.6-luna", "effort": "xhigh" },
      "finalReviewer": { "model": "codex2api/gpt-5.6-luna", "effort": "xhigh" }
    },
    "deepseek-glm": {
      "$comment": "原有 DeepSeek + GLM 分工,切换 activeModelProfile 即可恢复。",
      "main": { "model": "deepseek/deepseek-v4-pro", "effort": "high" },
      "prd": { "model": "zai/glm-5.2", "effort": "high" },
      "dev": { "model": "deepseek/deepseek-v4-flash", "effort": "high" },
      "reviewer": { "model": "zai/glm-5.2", "effort": "high" },
      "finalReviewer": { "model": "zai/glm-5.2", "effort": "high" }
    }
  },
  "providers": {
    "$comment": "仅需在这里注册非 Pi builtin provider。codex2api 由 Pi 自身配置提供,无需重复 endpoint/key。",
    "deepseek": {
      "baseUrl": "https://api.deepseek.com",
      "apiKeyEnv": "DEEPSEEK_API_KEY",
      "api": "openai-completions",
      "thinkingFormat": "deepseek"
    },
    "zai": {
      "baseUrl": "https://api.z.ai/api/coding/paas/v4",
      "apiKeyEnv": "GLM5_2_API_KEY",
      "api": "openai-completions",
      "thinkingFormat": "zai"
    }
  },
  "build": {
    "verifyCommand": "",
    "$comment_verify": "每个子任务关闭及最终验收都会执行的唯一预配置命令。留空是配置错误:/execute、claim、close、run_verify 都会直接拒绝。可用 /wf verify <cmd> 按需求覆盖。",
    "commitPrefix": "subtask"
  },
  "execute": {
    "$comment": "主 session 自己是只读经理,用 pi-subagents 调 dev/reviewer；所有角色模型由 activeModelProfile 统一决定。",
    "driver": "bd",
    "$comment_driver": "保留字段:目前只实现 bd。",
    "maxParallel": 1,
    "$comment_maxParallel": "安全上限固定为 1；确定性 worktree handoff 完成前禁止并行 writer。",
    "pollIntervalMs": 2000,
    "$comment_pollIntervalMs": "死字段:当前实现不轮询,仅为兼容。"
  }
}
