{
  "name": "pi-workflow",
  "version": "0.1.0",
  "description": "plan/build 两模式需求流水线:GLM PRD writer、Beads task、串行 dev/reviewer、确定性验证与 GLM final review。",
  "keywords": [
    "pi-package"
  ],
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "test:v1": "node --experimental-strip-types test/workflow-logic.test.ts",
    "test:v2": "node -e \"console.error('V2 workspace tests are not available until E01-T3.'); process.exit(1)\"",
    "test": "npm run test:v1 && npm run test:v2",
    "typecheck:v1": "tsc --project tsconfig.json",
    "typecheck:v2": "node -e \"console.error('V2 workspace typechecking is not available until E01-T3.'); process.exit(1)\"",
    "typecheck": "npm run typecheck:v1 && npm run typecheck:v2",
    "build:v2": "tsc --build tsconfig.v2.json",
    "check": "npm test && npm run typecheck",
    "skills:install": "node scripts/install-skills.mjs",
    "skills:uninstall": "node scripts/uninstall-skills.mjs",
    "skills:list": "node scripts/install-skills.mjs --list"
  },
  "pi": {
    "extensions": [
      "./extensions/workflow.ts",
      "./extensions/cache.ts"
    ],
    "skills": [
      "./skills"
    ],
    "subagents": {
      "agents": [
        "./.pi/agents"
      ]
    }
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.81.0",
    "@types/node": "^22.20.1",
    "typebox": "^1.1.38",
    "typescript": "^5.6.0"
  }
}
